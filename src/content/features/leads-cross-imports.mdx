export const meta = {
	title      : 'Cross import detection',
	description: 'Import leads with confidence using our cross-import detection mechanism to avoid duplicates or spam traps',
	feature    : {
		group: "leads",
		order: 5,
	}
}
